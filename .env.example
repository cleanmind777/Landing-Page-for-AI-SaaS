# Copy this file to .env.local and fill in your values.
# .env.local is git-ignored and used for local development.

# Optional: public site URL (e.g. for analytics or canonical links)
# NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Add any API keys or secrets below when needed:
# NEXT_PUBLIC_EXAMPLE_API_KEY=
# EXAMPLE_SECRET=
